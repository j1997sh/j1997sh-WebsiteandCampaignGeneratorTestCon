document.addEventListener('cp-admin-ready',async()=>{const {sb,orgId}=window.CP_ADMIN,id=new URLSearchParams(location.search).get('id'),esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));if(!id)return;const r=await sb.rpc('org_admin_account_detail',{p_org:orgId,p_account:id});if(r.error){adminAccountDetailMessage.innerHTML=`<div class="state-banner error">${esc(r.error.message)}</div>`;return}const d=r.data,a=d.account;document.querySelector('.admin-topbar h1').textContent=a.name;adminAccountDetail.innerHTML=`<div class="admin-kpi-grid compact"><div class="admin-kpi"><strong>${d.people_count}</strong><span>People</span></div><div class="admin-kpi"><strong>${d.actions_count}</strong><span>Actions</span></div><div class="admin-kpi"><strong>${d.responses_count}</strong><span>Survey responses</span></div></div><div class="admin-two-col"><section class="admin-panel"><h3>Websites</h3>${d.websites.map(w=>`<div class="admin-list-row"><div><strong>${esc(w.name)}</strong><small>${esc(w.area||'')} · ${w.status}</small></div></div>`).join('')||'<p class="muted">None</p>'}</section><section class="admin-panel"><h3>Campaigns</h3>${d.campaigns.map(c=>`<div class="admin-list-row"><div><strong>${esc(c.name)}</strong><small>${c.status} · ${c.supporter_count||0} supporters</small></div></div>`).join('')||'<p class="muted">None</p>'}</section></div><section class="admin-panel"><h3>Surveys</h3>${d.surveys.map(s=>`<div class="admin-list-row"><div><strong>${esc(s.name)}</strong><small>${s.status} · ${s.response_count||0} responses</small></div></div>`).join('')||'<p class="muted">None</p>'}</section>`});