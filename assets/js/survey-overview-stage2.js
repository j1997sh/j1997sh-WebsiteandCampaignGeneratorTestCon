(async function(){'use strict';const sb=window.cpSupabase,root=document.getElementById('surveyOverviewStage2'),id=new URLSearchParams(location.search).get('id'),esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const [sr,qr,rr,wr,cr,pr]=await Promise.all([sb.from('surveys').select('*').eq('id',id).single(),sb.from('survey_questions').select('*').eq('survey_id',id).order('position'),sb.from('survey_responses').select('*').eq('survey_id',id).order('created_at',{ascending:false}),sb.from('websites').select('id,name').eq('selected_survey_id',id),sb.from('campaigns').select('id,name').eq('survey_id',id),sb.from('people').select('*')]);if(sr.error){root.innerHTML='<div class="empty-state-card"><h3>Survey not found</h3></div>';return}const s=sr.data,qs=qr.data||[],responses=rr.data||[],people=Object.fromEntries((pr.data||[]).map(p=>[p.id,p]));const val=v=>Array.isArray(v)?v.join(', '):String(v??'');root.innerHTML=`<div class="page-intro"><div><span class="status-chip ${s.status==='published'?'published':'draft'}">${s.status==='published'?'Published':'Draft'}</span><h2>${esc(s.name)}</h2><p>${responses.length} responses · ${qs.filter(q=>q.enabled).length} active questions</p></div><a class="btn" href="survey-editor.html?id=${s.id}">Edit survey</a></div><div class="dashboard-grid"><section class="panel"><h3>Where it’s used</h3>${(wr.data||[]).map(w=>`<div class="linked-item"><span><strong>Website</strong><small>${esc(w.name)}</small></span><a class="btn secondary small" href="website-overview.html?id=${w.id}">Open</a></div>`).join('')}${(cr.data||[]).map(c=>`<div class="linked-item"><span><strong>Campaign</strong><small>${esc(c.name)}</small></span><a class="btn secondary small" href="campaign-overview.html?id=${c.id}">Open</a></div>`).join('')||(!(wr.data||[]).length?'<p class="muted">Not currently used.</p>':'')}</section><section class="panel"><h3>Responses</h3>${responses.length?responses.map(r=>{const p=people[r.person_id]||{};return `<article class="response-card"><div class="response-card-head"><div><strong>${esc([p.first_name,p.last_name].filter(Boolean).join(' ')||p.email||'Anonymous')}</strong><small>${new Date(r.created_at).toLocaleString()} · ${esc(r.source||'website')}</small></div>${r.person_id?`<a class="btn secondary small" href="person.html?id=${r.person_id}">View person</a>`:''}</div><dl class="response-answers">${qs.map(q=>`<div><dt>${esc(q.label)}</dt><dd>${esc(val((r.answers||{})[q.id])||'—')}</dd></div>`).join('')}</dl></article>`}).join(''):'<p class="muted">No responses yet.</p>'}</section></div>`})();
